What is the difference between a function and a procedure in PostgreSQL?

Функція - це щось, що дає нам результат(якесь значення), а 
процедура- це щось, що запускає дію, проте не зобов'язана повертати результат.


Can a trigger be executed manually? Why or why not?

ні, тригер не можна виконувати вручну, він автоматично запускається 
тоді коли відбувається та дія яка прописана для його активації (update, delete, insert).

What are the advantages and disadvantages of storing business logic inside the database?


У базі даних добре зберігати прості правила цілісності: NOT NULL, foreign key, UNIQUE, CHECK.
Вони гарантують, що дані завжди будуть коректними, навіть якщо до бази звертаються різні системи 
або хтось щось змінює вручну. Недоліки зберігання логіки в базі даних: ваще тестувати, логіка прив’язується
до конкретної бд, можуть виникнути проблеми в масштабуванні. Складну бізнес-логіку зазвичай краще тримати 
в коді застосунку, бо так її легше писати, тестувати й змінювати.
Підгрунття для відповіді взяла з дискусії на сайті stack overflow.